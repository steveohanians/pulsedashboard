```bash
# STEP 1: Add Brand Signals page link to dashboard navigation
# This is a minimal, safe change that only adds a link without breaking anything
# We're adding it to the existing navigation in the dashboard.tsx file
```

```jsx
// FILE: client/src/pages/dashboard.tsx
// 
// INSTRUCTIONS FOR REPLIT AGENT:
// 1. Find the desktop navigation section (around line 800-900)
// 2. Look for the section with metricNames.map() that creates the navigation buttons
// 3. AFTER the metricNames list but BEFORE the Admin Panel section
// 4. ADD the following Brand Signals link
// 5. Do NOT modify any existing navigation items
// 6. This is ONLY adding a new link, not changing navigation structure

// Find this section in dashboard.tsx:
{metricNames.map((metricName) => (
  <li key={metricName}>
    <button
      onClick={() => handleNavigationClick(metricName)}
      className={`w-full text-left p-2 rounded-lg transition-colors text-xs ${
        activeSection === metricName
          ? "bg-slate-100 text-primary"
          : "text-slate-700 hover:bg-slate-100 hover:text-primary"
      }`}
    >
      {metricName}
    </button>
  </li>
))}

// RIGHT AFTER the above section, ADD THIS:
<li className="my-4">
  <hr className="border-slate-200" />
</li>
<li>
  <Link href="/brand-signals">
    <button className="w-full text-left p-2 rounded-lg transition-colors text-xs text-slate-700 hover:bg-slate-100 hover:text-primary">
      <TrendingUp className="h-3 w-3 inline mr-2" />
      Brand Signals
    </button>
  </Link>
</li>

// Make sure the Admin Panel section stays AFTER this new link

// ALSO add the import at the top of the file if TrendingUp is not already imported:
import { TrendingUp } from "lucide-react";

// IMPORTANT: 
// - Only ADD the Brand Signals link
// - Do NOT modify existing navigation structure
// - Do NOT change how Vitals navigation works
// - This is just adding a simple link that goes to /brand-signals
// - The link uses the same styling as Admin Panel link
```

```bash
# EXPECTED RESULT:
# - Dashboard navigation will show "Brand Signals" link below the metrics
# - Clicking it will attempt to navigate to /brand-signals (will 404 for now, that's OK)
# - All existing navigation continues to work exactly as before
# - No breaking changes to current functionality

# TEST:
# 1. Dashboard should load normally
# 2. All Vitals navigation should work as before
# 3. New "Brand Signals" link should be visible
# 4. Clicking it will try to go to /brand-signals (404 is expected for now)
```