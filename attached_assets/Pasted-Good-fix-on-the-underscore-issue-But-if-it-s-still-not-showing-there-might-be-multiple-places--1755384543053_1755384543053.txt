Good fix on the underscore issue! But if it's still not showing, there might be **multiple places** that need the same fix. Let's check if the chart is receiving and processing Industry_Avg correctly.

## Replit Prompt: Check Chart Component Data Processing

```typescript
// Add this debug to client/src/components/charts/metrics-chart.tsx
// Find the component's main render logic (inside the MetricsChart component)
// Add this RIGHT AT THE START of the component function, after the props destructuring:

export default function MetricsChart({ title, data, metricName, timePeriod, ...props }) {
  // ADD THIS DEBUG BLOCK RIGHT HERE:
  console.error(`🔍 METRICS-CHART DEBUG for ${title}:`, {
    dataKeys: Object.keys(data || {}),
    hasIndustry_Avg: 'Industry_Avg' in (data || {}),
    hasIndustryAvg: 'Industry Avg' in (data || {}),
    Industry_Avg_value: data?.['Industry_Avg'],
    IndustryAvg_value: data?.['Industry Avg'],
    allData: JSON.stringify(data)
  });
  
  // Check what chartData is created
  const debugChartData = Object.entries(data || {})
    .filter(([key]) => key !== 'Client')
    .map(([name, value]) => {
      console.error(`  📊 Processing: ${name} = ${value}`);
      return { name, value: value || 0 };
    });
  
  console.error(`  📈 Final chart data for ${title}:`, JSON.stringify(debugChartData));
  // END DEBUG BLOCK

  // REST OF THE COMPONENT CONTINUES AS NORMAL...
  // Don't modify anything else
```

**Also check for multiple key format issues - add this search:**
```typescript
// Search the entire metrics-chart.tsx file for these patterns:
// 1. Search for: "Industry Avg" (with space)
// 2. Search for: "Industry_Avg" (with underscore)  
// 3. Search for: shouldConvertToPercentage

// Make sure ALL occurrences use "Industry_Avg" with underscore
// Replace any "Industry Avg" with "Industry_Avg"
```

**Guardrails:**
- Add debug at the VERY START of the component
- Don't modify the actual chart rendering
- Check for both key formats
- Shows what data is being processed
- Search for ALL occurrences of the key name

After this, search logs for "🔍 METRICS-CHART DEBUG" and share what keys are actually present in the data.