Fix GA4 Service Account Dropdown Without Changing Components

The NativeSelect is working everywhere else. The issue is likely just missing data or props. Let's fix it properly.

**Step 1: Revert any component changes in GA4IntegrationPanel**

In `/client/src/components/admin/GA4IntegrationPanel.tsx`, make sure it's using NativeSelect like everything else:

```typescript
import { NativeSelect } from "@/components/ui/native-select";

// The dropdown should be:
<NativeSelect
  name="serviceAccountId"
  value={selectedServiceAccountId || ""}
  onChange={(e) => {
    setSelectedServiceAccountId(e.target.value);
    onServiceAccountUpdate?.(e.target.value);
  }}
  options={[
    { value: "", label: "Select a service account" },
    ...(serviceAccounts || [])
      .filter(sa => sa.isActive)
      .map(sa => ({
        value: sa.id,
        label: `${sa.email} (${sa.projectId})`
      }))
  ]}
  placeholder="Select a service account"
/>
Step 2: Make sure the query is actually fetching data
In /client/src/pages/admin-panel.tsx, check that the query is correct:
typescriptconst { data: ga4ServiceAccounts } = useQuery<any[]>({
  queryKey: ['/api/admin/ga4-service-accounts'],  // Make sure this matches the actual endpoint
  queryFn: async () => {
    return await apiRequest('GET', '/api/admin/ga4-service-accounts');
  },
  enabled: user?.role === "Admin",
});
Step 3: Check if we're passing the props correctly
In admin-panel.tsx where GA4IntegrationPanel is used:
typescript<GA4IntegrationPanel 
  clientId={editingItem?.id || null}
  currentGA4PropertyId={editingItem?.ga4PropertyId || ""}
  serviceAccounts={ga4ServiceAccounts || []}  // Make sure this is being passed
  onGA4PropertyUpdate={(propertyId) => {
    const hiddenInputId = editingItem?.id ? `hidden-gaPropertyId-${editingItem.id}` : 'hidden-gaPropertyId-new';
    const input = document.querySelector(`#${hiddenInputId}`) as HTMLInputElement;
    if (input) {
      input.value = propertyId;
    }
  }}
  onServiceAccountUpdate={(serviceAccountId) => {
    const hiddenInputId = editingItem?.id ? `hidden-serviceAccount-${editingItem.id}` : 'hidden-serviceAccount-new';
    const input = document.querySelector(`#${hiddenInputId}`) as HTMLInputElement;
    if (input) {
      input.value = serviceAccountId;
    }
  }}
/>