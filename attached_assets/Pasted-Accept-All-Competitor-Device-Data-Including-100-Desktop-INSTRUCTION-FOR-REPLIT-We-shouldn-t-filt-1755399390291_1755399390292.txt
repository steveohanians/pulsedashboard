Accept All Competitor Device Data (Including 100% Desktop)
INSTRUCTION FOR REPLIT:
We shouldn't filter out competitors just because they have 100% Desktop - that might be their actual data from SEMrush. Let's show ALL competitor data as it comes from the source.
MODIFY /client/src/services/unifiedDataService.ts - UPDATE COMPETITOR VALIDATION:
FIND THIS SECTION (around line 520-560):
typescript      // Ensure we have both Desktop and Mobile
      if (dataFound && devices.length > 0) {
        // Check if we're missing Mobile or Desktop
        const hasDesktop = devices.find(d => d.name === 'Desktop');
        const hasMobile = devices.find(d => d.name === 'Mobile');
REPLACE THE VALIDATION SECTION WITH:
typescript      // Process whatever device data we have
      if (dataFound && devices.length > 0) {
        const hasDesktop = devices.find(d => d.name === 'Desktop');
        const hasMobile = devices.find(d => d.name === 'Mobile');
        
        // If we only have Desktop, add Mobile as 0
        if (hasDesktop && !hasMobile) {
          devices.push({
            name: 'Mobile',
            value: 0,
            percentage: 0,
            color: this.getDeviceColor('Mobile')
          });
          debugLog('UNIFIED', `Competitor ${competitor.domain} is Desktop-only (100% Desktop, 0% Mobile)`);
        }
        // If we only have Mobile, add Desktop as 0
        else if (!hasDesktop && hasMobile) {
          devices.push({
            name: 'Desktop',
            value: 0,
            percentage: 0,
            color: this.getDeviceColor('Desktop')
          });
          debugLog('UNIFIED', `Competitor ${competitor.domain} is Mobile-only (0% Desktop, 100% Mobile)`);
        }
        
        // Normalize percentages to ensure they sum to 100
        const total = devices.reduce((sum, d) => sum + d.value, 0);
        if (total > 0 && Math.abs(total - 100) > 1) {
          devices.forEach(device => {
            device.value = Math.round((device.value / total) * 1000) / 10;
            device.percentage = device.value;
          });
        }
      }
      
      // Add to result if we have ANY device data
      if (dataFound && devices.length > 0) {
        // Sort devices to ensure Desktop comes before Mobile
        devices.sort((a, b) => a.name === 'Desktop' ? -1 : 1);
        
        result.push({
          sourceType: `Competitor_${competitor.id}`,
          label: this.cleanDomainName(competitor.domain),
          devices: devices
        });
        
        debugLog('UNIFIED', `✅ Added competitor ${competitor.domain} to device distribution`, { 
          devices: devices.map(d => `${d.name}: ${d.value}%`).join(', ')
        });
      } else {
        debugLog('UNIFIED', `❌ No device data found for competitor ${competitor.domain}`);
      }
ALSO UPDATE THE CONDITION AT THE BOTTOM (around line 570):
typescript      // Change from:
      // if (dataFound && devices.length >= 2) {
      
      // To:
      if (dataFound && devices.length > 0) {
This fix:

Accepts ALL competitor data - including sites that are 100% Desktop
Adds Mobile: 0% when only Desktop data exists (and vice versa)
Shows the authentic data from SEMrush without filtering
Ensures both Desktop and Mobile values exist for chart compatibility

Now ALL three competitors should show:

baunfire.com: Desktop 100%, Mobile 0%
adidas.com: Desktop 31.5%, Mobile 68.5%
liquidagency.com: Desktop 100%, Mobile 0%

This is the REAL data from SEMrush - some B2B sites really are 100% desktop traffic!